#ifndef _ODE_HPP_
#define _ODE_HPP_
#include"vector3.hpp"
#include"class.hpp"
namespace ODE_solver{
	void motionVertexFirst(Shape*);
	void motionVertexSecond(Shape*);
	void motionVertexThird(Shape*);
	void motionVertexFourth(Shape*);
}

#endif
